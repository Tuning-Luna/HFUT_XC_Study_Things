# 数据库实验操作指引

**数据库名称**：`jwgl2000000000`（学号：2000000000）

**环境要求**：MySQL 8.0+ + Navicat 15.0+

**SQL 文件清单**：共 14 个 `.sql` 文件，按编号顺序执行。

---

## 目录

- [实验一：数据库的基本操作](#实验一数据库的基本操作)
  - [1. 创建数据库](#1-创建数据库)
  - [2. 创建数据表](#2-创建数据表)
  - [3. 修改列的定义](#3-修改列的定义)
  - [4. 建立索引](#4-建立索引)
  - [5. 删除索引](#5-删除索引)
  - [6. 备份和恢复数据库](#6-备份和恢复数据库)
  - [7. 插入数据](#7-插入数据)
  - [8. 更新与删除操作](#8-更新与删除操作)
  - [9. 简单查询](#9-简单查询)
  - [10. 连接查询](#10-连接查询)
  - [11. 嵌套和集合查询](#11-嵌套和集合查询)
  - [12. 视图](#12-视图)
- [实验二：触发器、存储过程](#实验二触发器存储过程)
  - [1. Class 表添加字段](#1-class-表添加字段)
  - [2. 存储过程 p1 和 p2](#2-存储过程-p1-和-p2)
  - [3. 触发器 instrg1 和 update1](#3-触发器-instrg1-和-update1)
- [实验三：安全性、备份与恢复](#实验三安全性备份与恢复)
  - [1. 用户与权限管理](#1-用户与权限管理)
  - [2. 备份与恢复](#2-备份与恢复)
  - [3. 触发器 S_student](#3-触发器-s_student)
- [附录：截图清单汇总](#附录截图清单汇总)

---

## 实验一：数据库的基本操作

### 1. 创建数据库

| 操作 | 说明 |
|---|---|
| **执行文件** | `01_create_database.sql` |
| **关键 SQL** | `CREATE DATABASE jwgl2000000000 ...` |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图1-1 | Navicat 中执行 `01_create_database.sql` 后的结果面板 | 显示 `Query OK, 1 row affected` |
| 图1-2 | Navicat 左侧数据库列表中出现 `jwgl2000000000` | 展开后可看到空的数据库 |

---

### 2. 创建数据表

| 操作 | 说明 |
|---|---|
| **执行文件** | `02_create_tables.sql` |
| **关键 SQL** | `CREATE TABLE Class (...)`, `CREATE TABLE Student (...)`, `CREATE TABLE Course (...)`, `CREATE TABLE SC (...)` |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图2-1 | 执行完毕后 Navicat 显示四张表的表列表 | 左侧展开数据库 → 表，显示 Class、Student、Course、SC |
| 图2-2 | `DESCRIBE Class;` 的执行结果 | 显示 Class 表各字段的类型和约束 |
| 图2-3 | `DESCRIBE Student;` 的执行结果 | 显示 Student 表各字段的类型和约束（含 CHECK 约束） |
| 图2-4 | `DESCRIBE Course;` 的执行结果 | 显示 Course 表各字段的类型和约束 |
| 图2-5 | `DESCRIBE SC;` 的执行结果 | 显示 SC 表的复合主键和外键 |

---

### 3. 修改列的定义

| 操作 | 说明 |
|---|---|
| **执行文件** | `03_alter_columns.sql` |

此文件包含 4 个操作，建议**逐条选中执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图3-1 | 执行 `ALTER TABLE Student MODIFY COLUMN Sname VARCHAR(20) NOT NULL` 后，`DESCRIBE Student;` 的结果 | Sname 类型显示为 `varchar(20)` |
| 图3-2 | 执行 `ALTER TABLE Course ADD CONSTRAINT chk_semester CHECK (Semester BETWEEN 1 AND 10)` 后，`SELECT * FROM INFORMATION_SCHEMA.CHECK_CONSTRAINTS WHERE TABLE_NAME='Course';` 的结果 | 显示 CHECK 约束为 `semester between 1 and 10` |
| 图3-3 | 执行 `ALTER TABLE Course DROP COLUMN Semester` 后，`DESCRIBE Course;` 的结果 | Semester 列已消失 |
| 图3-4 | 执行 `ALTER TABLE Course ADD COLUMN Semester INT NULL` 后，`DESCRIBE Course;` 的结果 | Semester 列重新出现 |

---

### 4. 建立索引

| 操作 | 说明 |
|---|---|
| **执行文件** | `04_indexes.sql`（执行到"步骤5"之前） |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图4-1 | 执行 `SHOW INDEX FROM Student;` 的结果 | 显示 `idx_student_sname_desc` 降序索引 |
| 图4-2 | 执行 `SHOW INDEX FROM Course;` 的结果 | 显示 `idx_course_cname_unique` 唯一索引 |
| 图4-3 | 执行 `SHOW INDEX FROM SC;` 的结果 | 显示 `idx_sc_sno_cno_grade` 复合索引 |
| 图4-4 | `04_indexes.sql` 中关于聚集索引的注释说明截图 | 展示对 MySQL InnoDB 中聚集索引=主键的理解 |

---

### 5. 删除索引

| 操作 | 说明 |
|---|---|
| **执行文件** | `04_indexes.sql`（步骤5部分） |
| **关键 SQL** | `DROP INDEX idx_course_cname_unique ON Course;` |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图5-1 | 执行删除语句后，`SHOW INDEX FROM Course;` 的结果 | `idx_course_cname_unique` 索引已消失 |

---

### 6. 备份和恢复数据库

| 操作 | 说明 |
|---|---|
| **执行文件** | 参考 `14_backup_restore.sql` |
| **执行位置** | 操作系统终端（cmd / PowerShell），非 Navicat 查询窗口 |

**操作步骤**：

```bash
# 备份（在终端执行）
mysqldump -u root -p jwgl2000000000 > D:\jwgl2000000000_backup.sql
```

```bash
# 恢复（在终端执行）
mysql -u root -p jwgl2000000000 < D:\jwgl2000000000_backup.sql
```

> **也可以使用 Navicat 可视化方式**：右键数据库 → 转储SQL文件 / 运行SQL文件

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图6-1 | 终端中执行 `mysqldump` 命令 | 显示命令输入（可对密码提示打码） |
| 图6-2 | Windows 资源管理器中 `D:\jwgl2000000000_backup.sql` 文件存在 | 显示文件大小和日期 |
| 图6-3 | Navicat 右键导出界面（可选替代图6-1/6-2） | 显示"转储SQL文件"操作过程 |
| 图6-4 | 备份文件的前几行内容（用记事本打开截图） | 显示包含建表语句等 |

---

### 7. 插入数据

| 操作 | 说明 |
|---|---|
| **执行文件** | `05_insert_data.sql` |

> ⚠ **注意**：SC 表中有课程号 `0007` 的数据，但 Course 表中无此课程。脚本使用 `SET FOREIGN_KEY_CHECKS=0` 临时禁用外键检查。请在实验报告中说明此情况。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图7-1 | `SELECT * FROM Class;` 的结果 | 显示 4 条班级记录 |
| 图7-2 | `SELECT * FROM Student;` 的结果 | 显示 4 条学生记录 |
| 图7-3 | `SELECT * FROM Course;` 的结果 | 显示 6 条课程记录 |
| 图7-4 | `SELECT * FROM SC;` 的结果 | 显示 9 条选课记录（含 NULL 成绩和 0007 课程） |

---

### 8. 更新与删除操作

| 操作 | 说明 |
|---|---|
| **执行文件** | `06_update_delete.sql` |

此文件包含 5 个操作，建议**逐条选中执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图8-1 | 操作1执行后，`SELECT Sno, Sname, Sage, ClsNO FROM Student;` 的结果 | CS01 中年龄<20 的学生班级号变为 CS02 |
| 图8-2 | 操作2执行后，`SELECT * FROM Student;` 的结果 | 年龄>20 且班级为 CS02 的学生被删除 |
| 图8-3 | 操作3执行后，`SELECT * FROM Student WHERE Sname='张三';` 的结果 | 新增的张三记录 |
| 图8-4 | 操作4执行后，`SELECT Sno, Sname, Sage, Saddr FROM Student ORDER BY Sage;` 的结果 | 年龄最小的学生 Saddr 变为 NULL |
| 图8-5 | 操作5执行后，`SELECT * FROM Class;` 和 `SELECT Sno, Sname, ClsNO FROM Student;` 的结果 | 平均年龄最小的班级号改为 GL01，学生表级联更新 |

---

### 9. 简单查询

| 操作 | 说明 |
|---|---|
| **执行文件** | `07_simple_queries.sql` |

共 6 条查询，建议**逐条执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图9-1 | 查询1结果 | 计算机应用专业的学生学号和姓名 |
| 图9-2 | 查询2结果 | 选修了课程的学生学号（去重后） |
| 图9-3 | 查询3结果 | 0001 课程学生成绩（降序排列，同分按学号升序） |
| 图9-4 | 查询4结果 | 0001 课程 80-90 分段，成绩×0.75 |
| 图9-5 | 查询5结果 | 计算机应用和数学专业姓"张"的学生 |
| 图9-6 | 查询6结果 | 缺少成绩的学生学号和课程号（Grade IS NULL） |

---

### 10. 连接查询

| 操作 | 说明 |
|---|---|
| **执行文件** | `08_join_queries.sql` |

共 4 条查询，建议**逐条执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图10-1 | 查询1结果 | 每个学生及其选修课程（LEFT JOIN，包含未选课学生） |
| 图10-2 | 查询2结果 | 学生学号、姓名、课程名、成绩（三表 JOIN） |
| 图10-3 | 查询3结果 | 0001 课程成绩>90 的学生学号、姓名、成绩 |
| 图10-4 | 查询4结果 | 每门课的间接先行课（自连接，两次 JOIN） |

---

### 11. 嵌套和集合查询

| 操作 | 说明 |
|---|---|
| **执行文件** | `09_nested_queries.sql` |

共 9 条查询，建议**逐条执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图11-1 | 查询1结果 | 0001 课程成绩高于"张三"的学生 |
| 图11-2 | 查询2结果 | 其他专业中年龄小于计算机应用专业最大年龄的学生 |
| 图11-3 | 查询3结果 | 没有选修 0002 课程的学生姓名 |
| 图11-4 | 查询4结果 | 选修了全部课程的学生姓名 |
| 图11-5 | 查询5结果 | 选修课超过 3 门的学生学号 |
| 图11-6 | 查询6结果 | 每个专业的学生人数统计 |
| 图11-7 | 查询7结果 | 每门课程的选课人数和最高分 |
| 图11-8 | 查询8结果 | 计算机专业考试成绩最高的学生姓名 |
| 图11-9 | 查询9结果 | 每个同学的必修课总学分和选修课总学分 |

---

### 12. 视图

| 操作 | 说明 |
|---|---|
| **执行文件** | `10_views.sql` |

共 13 个操作，建议**逐段执行并截图**。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图12-1 | `SELECT * FROM S_C_GRADE;` 的结果 | 视图 S_C_GRADE（学号、姓名、课名、成绩） |
| 图12-2 | `SELECT * FROM COMPUTE_AVG_GRADE;` 的结果 | 视图 COMPUTE_AVG_GRADE（CS 专业学生及其平均成绩） |
| 图12-3 | `DROP VIEW` 后 `SHOW FULL TABLES WHERE Table_type='VIEW';` 的结果 | COMPUTE_AVG_GRADE 已消失 |
| 图12-4 | `SELECT * FROM V_NUM_AVG;` 的结果 | 各专业人数和平均年龄 |
| 图12-5 | `SELECT * FROM V_YEAR;` 的结果 | 学生出生年份 |
| 图12-6 | `SELECT * FROM V_AVG_S_G;` 的结果 | 每位学生的选课门数和平均成绩 |
| 图12-7 | `SELECT * FROM V_AVG_C_G;` 的结果 | 每门课程的选课人数和平均成绩 |
| 图12-8 | 查询8结果 | 平均成绩 ≥90 的学生学号、姓名、成绩 |
| 图12-9 | 查询9结果 | 各课成绩均大于课程平均分的学生 |
| 图12-10 | 查询10结果 | 按专业统计平均成绩 ≥80 的人数（降序） |
| 图12-11 | V_IS 视图操作：创建、UPDATE、SELECT 结果 | UPDATE 操作及结果 |
| 图12-12 | V_IS 视图 INSERT 操作及 `SELECT * FROM V_IS;` 结果 | 新增学生记录 |
| 图12-13 | 视图 V_AVG_S_G 不可更新说明 | 在实验报告中用文字说明原因，可附上执行 `UPDATE V_AVG_S_G` 的报错截图 |
| 图12-14 | `SHOW FULL TABLES WHERE Table_type='VIEW';` 最终结果 | 所有已创建的视图列表 |

---

## 实验二：触发器、存储过程

### 1. Class 表添加字段

| 操作 | 说明 |
|---|---|
| **执行文件** | `11_procedures.sql`（第一部分） |
| **关键 SQL** | `ALTER TABLE Class ADD COLUMN stotal INT NULL DEFAULT 0, ADD COLUMN monitor VARCHAR(10) NULL;` |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图13-1 | `DESCRIBE Class;` 的结果 | 显示新增的 stotal 和 monitor 字段 |

---

### 2. 存储过程 p1 和 p2

| 操作 | 说明 |
|---|---|
| **执行文件** | `11_procedures.sql`（第二部分） |

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图13-2 | 存储过程 p1 的创建语句 + `CALL p1();` 的执行结果 | 显示所有年龄<21 的男同学 |
| 图13-3 | 存储过程 p2 的创建语句 + `CALL p2('20190101');` 的执行结果 | 显示与 20190101 同龄的所有同学 |
| 图13-4 | Navicat 函数列表中显示的 p1、p2 存储过程 | 展开数据库 → 函数，可见两个存储过程 |

---

### 3. 触发器 instrg1 和 update1

| 操作 | 说明 |
|---|---|
| **执行文件** | `12_triggers.sql`（第一部分：instrg1 和 update1） |

**测试 instrg1（INSERT 触发器）**：

```sql
-- 测试前先查看班级人数
SELECT ClsNO, ClsName, stotal FROM Class;

-- 插入一名新学生
INSERT INTO Student (Sno, Sname, Ssex, Sage, ClsNO) VALUES
    ('20001001', '测试生', '男', 20, 'CS01');

-- 再次查看班级人数，CS01 的 stotal 应 +1
SELECT ClsNO, ClsName, stotal FROM Class;

-- 清理测试数据
DELETE FROM Student WHERE Sno = '20001001';
```

**测试 update1（UPDATE 触发器）**：

```sql
-- 测试前先查看班级人数
SELECT ClsNO, ClsName, stotal FROM Class;

-- 将某学生从 CS01 调到 CS02
UPDATE Student SET ClsNO = 'CS02' WHERE Sno = '20190101';

-- 查看班级人数，CS01 应 -1，CS02 应 +1
SELECT ClsNO, ClsName, stotal FROM Class;

-- 还原
UPDATE Student SET ClsNO = 'CS01' WHERE Sno = '20190101';
```

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图14-1 | 触发器 instrg1 的创建语句 | Navicat 中成功执行的提示 |
| 图14-2 | INSERT 测试前后 `SELECT ... FROM Class` 的结果对比 | stotal 变化前后的对比（两张图拼成一张） |
| 图14-3 | 触发器 update1 的创建语句 | Navicat 中成功执行的提示 |
| 图14-4 | UPDATE 测试前后 `SELECT ... FROM Class` 的结果对比 | stotal 在两个班级间变化 |
| 图14-5 | `SHOW TRIGGERS;` 的结果 | 显示所有已创建的触发器列表 |

---

## 实验三：安全性、备份与恢复

### 1. 用户与权限管理

| 操作 | 说明 |
|---|---|
| **执行文件** | `13_security.sql` |

> ⚠ **注意**：需以 root 或具有管理权限的账户执行。

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图15-1 | `SELECT User, Host FROM mysql.user WHERE User IN ('log1','log2','log3');` 的结果 | 三个用户已创建 |
| 图15-2 | `SHOW GRANTS FOR 'log1'@'localhost';` 的结果 | log1 的 SELECT 权限 |
| 图15-3 | `SHOW GRANTS FOR 'log2'@'localhost';` 的结果 | log2 的 SELECT + ROLE1 权限 |
| 图15-4 | `SHOW GRANTS FOR 'log3'@'localhost';` 的结果 | log3 的 SELECT + INSERT 权限 |
| 图15-5 | 用 **log2** 登录 Navicat，执行 `UPDATE Student SET Saddr='测试地址' WHERE Sno='20190101';` 的结果 | **执行成功**（log2 通过 ROLE1 拥有 UPDATE 权限） |
| 图15-6 | 用 **log3** 登录 Navicat，执行 `UPDATE Student SET Saddr='测试地址' WHERE Sno='20190101';` 的**报错截图** | **报错**：`UPDATE command denied to user 'log3'@'localhost'`（log3 无 UPDATE 权限） |
| 图15-7 | Navicat 用户管理界面（可选） | 显示 log1、log2、log3 三个用户及其权限设置 |

---

### 2. 备份与恢复

| 操作 | 说明 |
|---|---|
| **执行文件** | 参考 `14_backup_restore.sql` |

**操作流程**：

```bash
# 步骤①：在终端备份数据库
mysqldump -u root -p jwgl2000000000 > D:\jwgl2000000000_backup.sql

# 步骤②：在 Navicat 中删除 SC 表
# 执行：DROP TABLE IF EXISTS SC;
# 验证：SHOW TABLES;  -- SC 表已消失

# 步骤③：在终端恢复数据库
mysql -u root -p jwgl2000000000 < D:\jwgl2000000000_backup.sql

# 步骤④：在 Navicat 中验证恢复
# 执行：SELECT * FROM SC;  -- SC 表及其数据已恢复
```

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图16-1 | 终端中执行 `mysqldump` 备份命令 | （可对密码提示打码） |
| 图16-2 | `DROP TABLE IF EXISTS SC;` + `SHOW TABLES;` 的结果 | SC 表已删除 |
| 图16-3 | 终端中执行 `mysql` 恢复命令 | （可对密码提示打码） |
| 图16-4 | 恢复后 `SHOW TABLES;` + `SELECT * FROM SC;` 的结果 | SC 表和数据均已恢复 |

---

### 3. 触发器 S_student

| 操作 | 说明 |
|---|---|
| **执行文件** | `12_triggers.sql`（第二部分：S_student） |

**测试步骤**：

```sql
-- 测试 A：学号不以"20"开头 → 应被阻止
INSERT INTO Student (Sno, Sname, Ssex, Sage, ClsNO) VALUES
    ('19001001', '测试生A', '男', 20, 'CS01');
-- 预期报错：学生号前2位必须是20

-- 测试 B：学号以"20"开头 → 应成功插入
INSERT INTO Student (Sno, Sname, Ssex, Sage, ClsNO) VALUES
    ('20001001', '测试生B', '女', 21, 'CS02');
-- 预期成功，且 instrg1 触发器自动更新 CS02 的 stotal
```

**截图要求** ✅：

| 编号 | 截图内容 | 说明 |
|---|---|---|
| 图17-1 | 触发器 S_student 的创建语句 | Navicat 中成功执行的提示 |
| 图17-2 | 测试 A 的**报错截图** | 错误信息："学生号前2位必须是20" |
| 图17-3 | 测试 B 的**成功截图** + `SELECT * FROM Student WHERE Sname='测试生B';` 的结果 | 合法学生插入成功 |
| 图17-4 | 测试后 `SELECT ClsNO, stotal FROM Class;` 的结果 | CS02 的 stotal 已自动 +1（instrg1 生效） |
| 图17-5 | `SHOW TRIGGERS;` 的最终结果 | 显示全部 3 个触发器（instrg1、update1、S_student） |

---

## 附录：截图清单汇总

为方便在实验报告中按顺序排列，以下是全部需要截图的汇总表：

### 实验一截图（图1 ~ 图12）

| 编号 | 内容 | 来源文件 |
|---|---|---|
| 图1-1 | 数据库创建成功 | `01_create_database.sql` |
| 图1-2 | Navicat 数据库列表 | Navicat 界面 |
| 图2-1 | 四张表的表列表 | `02_create_tables.sql` |
| 图2-2 | DESCRIBE Class | `02_create_tables.sql` |
| 图2-3 | DESCRIBE Student | `02_create_tables.sql` |
| 图2-4 | DESCRIBE Course | `02_create_tables.sql` |
| 图2-5 | DESCRIBE SC | `02_create_tables.sql` |
| 图3-1 | Sname 改为 VARCHAR(20) | `03_alter_columns.sql` |
| 图3-2 | Semester 添加 CHECK(1-10) | `03_alter_columns.sql` |
| 图3-3 | Semester 删除后 | `03_alter_columns.sql` |
| 图3-4 | Semester 重新添加后 | `03_alter_columns.sql` |
| 图4-1 | Student 表索引 | `04_indexes.sql` |
| 图4-2 | Course 表唯一索引 | `04_indexes.sql` |
| 图4-3 | SC 表复合索引 | `04_indexes.sql` |
| 图4-4 | 聚集索引注释说明 | `04_indexes.sql` |
| 图5-1 | 唯一索引删除后 | `04_indexes.sql` |
| 图6-1 | mysqldump 备份命令 | 终端 |
| 图6-2 | 备份文件存在 | 资源管理器 |
| 图6-3 | Navicat 导出界面（可选） | Navicat 界面 |
| 图6-4 | 备份文件内容 | 记事本 |
| 图7-1 | SELECT * FROM Class | `05_insert_data.sql` |
| 图7-2 | SELECT * FROM Student | `05_insert_data.sql` |
| 图7-3 | SELECT * FROM Course | `05_insert_data.sql` |
| 图7-4 | SELECT * FROM SC | `05_insert_data.sql` |
| 图8-1 | UPDATE 班级号后 | `06_update_delete.sql` |
| 图8-2 | DELETE 后 | `06_update_delete.sql` |
| 图8-3 | INSERT 张三后 | `06_update_delete.sql` |
| 图8-4 | 最小年龄学生地址置空 | `06_update_delete.sql` |
| 图8-5 | 最小平均年龄班级改为 GL01 | `06_update_delete.sql` |
| 图9-1~9-6 | 6 条简单查询结果 | `07_simple_queries.sql` |
| 图10-1~10-4 | 4 条连接查询结果 | `08_join_queries.sql` |
| 图11-1~11-9 | 9 条嵌套查询结果 | `09_nested_queries.sql` |
| 图12-1~12-14 | 13 个视图操作结果 | `10_views.sql` |

### 实验二截图（图13 ~ 图14）

| 编号 | 内容 | 来源文件 |
|---|---|---|
| 图13-1 | DESCRIBE Class（含 stotal, monitor） | `11_procedures.sql` |
| 图13-2 | CALL p1() 结果 | `11_procedures.sql` |
| 图13-3 | CALL p2() 结果 | `11_procedures.sql` |
| 图13-4 | Navicat 函数列表 | Navicat 界面 |
| 图14-1 | instrg1 创建成功 | `12_triggers.sql` |
| 图14-2 | instrg1 测试（stotal 变化） | 手动测试 |
| 图14-3 | update1 创建成功 | `12_triggers.sql` |
| 图14-4 | update1 测试（stotal 变化） | 手动测试 |
| 图14-5 | SHOW TRIGGERS | `12_triggers.sql` |

### 实验三截图（图15 ~ 图17）

| 编号 | 内容 | 来源文件 |
|---|---|---|
| 图15-1 | 三个用户已创建 | `13_security.sql` |
| 图15-2 | SHOW GRANTS FOR log1 | `13_security.sql` |
| 图15-3 | SHOW GRANTS FOR log2 | `13_security.sql` |
| 图15-4 | SHOW GRANTS FOR log3 | `13_security.sql` |
| 图15-5 | log2 执行 UPDATE **成功** | 用 log2 登录测试 |
| 图15-6 | log3 执行 UPDATE **报错** | 用 log3 登录测试 |
| 图15-7 | Navicat 用户管理界面 | Navicat 界面 |
| 图16-1 | mysqldump 备份 | 终端 |
| 图16-2 | SC 表已删除 | `14_backup_restore.sql` |
| 图16-3 | mysql 恢复 | 终端 |
| 图16-4 | SC 表已恢复 | `14_backup_restore.sql` |
| 图17-1 | S_student 创建成功 | `12_triggers.sql` |
| 图17-2 | 学号不以"20"开头 → 报错 | 手动测试 |
| 图17-3 | 学号以"20"开头 → 成功 | 手动测试 |
| 图17-4 | stotal 联动更新验证 | 手动测试 |
| 图17-5 | SHOW TRIGGERS 最终结果 | `12_triggers.sql` |

---

## 执行顺序建议

```
01_create_database.sql          → 截图图1-1、图1-2
02_create_tables.sql            → 截图图2-1~图2-5
03_alter_columns.sql            → 截图图3-1~图3-4（逐条执行）
04_indexes.sql                  → 截图图4-1~图4-4、图5-1
05_insert_data.sql              → 截图图7-1~图7-4
06_update_delete.sql            → 截图图8-1~图8-5（逐条执行）
07_simple_queries.sql           → 截图图9-1~图9-6（逐条执行）
08_join_queries.sql             → 截图图10-1~图10-4（逐条执行）
09_nested_queries.sql           → 截图图11-1~图11-9（逐条执行）
10_views.sql                    → 截图图12-1~图12-14（分段执行）
11_procedures.sql               → 截图图13-1~图13-4
12_triggers.sql (instrg1, update1)  → 截图图14-1~图14-5 + 手动测试
13_security.sql                 → 截图图15-1~图15-7 + log2/log3 登录测试
14_backup_restore.sql           → 截图图16-1~图16-4 + 终端操作
12_triggers.sql (S_student)     → 截图图17-1~图17-5 + 手动测试
```

> 💡 **提示**：建议在每个 SQL 文件执行**之前**先完整阅读一遍注释，理解每条语句的作用。截图时将 SQL 语句和结果放在同一画面中，方便实验报告中对照展示。

# 实验一：启动 MAVROS 服务连接测试 — 操作总结

**时间：** 2026-06-18
**虚拟机：** Ubuntu 20.04 (ROS Noetic)，IP 192.168.252.129

---

## 完成内容

### 步骤一~四：环境准备 ✅
- 工作空间 `~/test1/student1/catkin_ws` 已创建并初始化
- `.bashrc` 已配置环境变量 `source ~/test1/student1/catkin_ws/devel/setup.bash`

### 步骤五：MAVROS 安装 ✅
- `ros-noetic-mavros`、`ros-noetic-mavros-extras`、`ros-noetic-mavros-msgs` 已安装
- ⚠️ 运行 MAVROS 需要连接飞控 + 数传（`/dev/ttyUSB0`），虚拟机当前无硬件

### 步骤七~八：gps_listener 节点 ✅
- 包路径：`~/test1/student1/catkin_ws/src/gps_listener/`
- 源代码：`src/gps_listener.cpp`，订阅 `/mavros/global_position/global` 话题获取 GPS
- CMakeLists.txt 已添加 `sensor_msgs` 依赖 + `add_executable` + `target_link_libraries`

### 步骤九：编译 ✅
- `catkin_make` 编译成功
- 可执行文件：`~/test1/student1/catkin_ws/devel/lib/gps_listener/gps_listener`

## 待完成
- 连接飞控硬件后运行：
  1. `roscore`
  2. `roslaunch mavros apm.launch fcu_url:=/dev/ttyUSB0:57600`
  3. `rosrun gps_listener gps_listener`
  4. 观察 GPS 经纬度/高度输出，验证链路正常

# 实验二：航线推送与航点解析文件 — 操作总结

**时间：** 2026-06-18
**虚拟机：** Ubuntu 20.04 (ROS Noetic)，IP 192.168.252.129

---

## 完成内容

### 步骤一：新建工程包 ✅
- 包路径：`~/test2/student2/catkin_ws/src/kadli/`
- 依赖项：roscpp, rospy, std_msgs, roslib, mavros_msgs

### 步骤二：航点文件 ✅
- 路径：`~/test2/student2/catkin_ws/src/kadli/config/xicaochang.waypoints`
- 格式：QGC WPL 110，6 个航点（home + takeoff + 3 waypoints + land）
- 坐标区域：合肥工业大学宣城校区附近 (31.77°N, 117.19°E)

### 步骤三~四：解析与推送代码 ✅
- 源文件：`src/speech_recognition.cpp`
- `parseWaypoints()`：逐行解析 QGC WPL 110 格式，填充 mavros_msgs::Waypoint
- `main()`：调用 parseWaypoints → 通过 `/mavros/mission/push` 服务推送航点

### 步骤五：编译 ✅
- CMakeLists.txt 已添加 `mavros_msgs` 依赖 + `add_executable` + `target_link_libraries`
- `catkin_make` 编译成功
- `.bashrc` 已更新为 `source ~/test2/student2/catkin_ws/devel/setup.bash`

## 待完成（需硬件）
- 连接飞控 + 数传，启动 MAVROS
- `rosrun kadli speech_recognition` 推送航点
- MissionPlanner 连接飞控验证航点已写入

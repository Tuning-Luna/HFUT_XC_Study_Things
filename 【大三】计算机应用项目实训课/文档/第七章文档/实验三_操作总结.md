# 实验三：语音识别控制系统与自动航线推送 — 操作总结

**时间：** 2026-06-18
**虚拟机：** Ubuntu 20.04 (ROS Noetic)，IP 192.168.252.129

---

## 完成内容

### 步骤一：编写代码 ✅
- 包路径：`~/test3/student3/catkin_ws/src/kadli/`
- 源文件：`src/speech_recognition.cpp` (192 行)

**实现功能：**
1. `parseWaypoints()` — 解析 QGC WPL 110 航点文件
2. 语音识别循环 — 通过 popen 调用 `sherpa-ncnn-alsa` 引擎，捕获识别文本
3. **关键词匹配（含容错）：**
   - 逸夫楼 / 一副楼 / 亦夫楼 / 易夫楼 / 一夫楼 / 一福楼 → yifulou.waypoints
   - 西操场 / 细操场 → xicaochang.waypoints
   - 图书馆 → tushuguan.waypoints
4. 航点推送 — 调用 `/mavros/mission/push` 服务
5. **"起飞"触发：** `/mavros/set_mode` 设为 AUTO + 关闭语音子进程
6. Launch 文件：`launch/speech_recognition.launch`

### 航点文件 ✅
- `config/yifulou.waypoints`, `config/xicaochang.waypoints`, `config/tushuguan.waypoints`（原始）
- `config/1.waypoints`, `config/2.waypoints`（教师提供，宣城坐标）

### 编译 ✅
- CMakeLists.txt + package.xml 已添加 `mavros_msgs` 依赖
- `catkin_make` 编译成功
- `.bashrc` 已更新为 test3 工作空间

## 待完成（需硬件）
- RK3566 连接飞控 + 麦克风
- 将 kadli 包部署到 RK3566 的 catkin_ws/src
- `roslaunch kadli speech_recognition.launch` 启动语音识别
- 喊口令"目标-图书馆/西操场/逸夫楼-起飞"测试

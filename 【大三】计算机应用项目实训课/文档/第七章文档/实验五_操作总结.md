# 实验五：航点修改与路径再规划 — 操作总结

**时间：** 2026-06-18
**虚拟机：** Ubuntu 20.04 (ROS Noetic)，IP 192.168.252.129

---

## 完成内容

### 步骤一：拉取飞控航点 ✅
- `waypointSet()` 函数已实现（1s 定时检查）
- 到达 1 号航点后通过 `/mavros/mission/pull` 拉取航点列表
- `waypoints_callback()` 订阅 `/mavros/mission/waypoints` 获取航点 → waypoints_read_vector
- 提取 10 号点坐标作为 Lat_Scope/Lon_Scope 和预设投弹点

### 步骤二：修改航点坐标 ✅
- `changeWaypoint()` 函数已实现：
  - 将 10 号点坐标改为识别到的目标坐标 (drop_coordinates_lat/lon)
  - `calculate_extended_point()` 计算 9→10 延长线上 11 号点坐标
  - 保持投弹航线为一条直线

### 步骤三：推送航点 + 设置当前航点 ✅
- 发现目标后 (find_goal==1)：
  - `wp_push_client` 推送修改后的 waypoints_send_vector
  - `wp_set_current_client` 设置 8 号点为当前航点

### 编译 ✅
- `catkin_make` 编译成功（coordinate_cal 包，三文件联编）

## 待完成（需硬件）
- RK3566 部署 coordinate_cal 包
- MissionPlanner 验证航点已更新

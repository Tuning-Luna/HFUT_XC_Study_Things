# 实验四：目标识别与地理坐标定位计算 — 操作总结

**时间：** 2026-06-18
**虚拟机：** Ubuntu 20.04 (ROS Noetic)，IP 192.168.252.129

---

## 完成内容

### 步骤一~二：模型部署与测试 ⚠️
- 需在 RK3566 泰山派上操作
- 部署 YOLOv5s .rknn 模型
- `roslaunch rknn_ros camera.launch` + `roslaunch rknn_ros yolov5.launch`

### 步骤三：坐标转换函数 ✅
- 包路径：`~/test4/student4/catkin_ws/src/coordinate_cal/`
- `position_coordinates()` 已完成：
  - 像素 → 直角坐标 (pixelToCentiMeters * rel_alt)
  - 直角 → 极坐标 (convertRectToPolar)
  - 叠加航向角 → 地球极坐标
  - 极坐标 → 地球直角坐标 (convertPolarToRect)
  - cm → 经纬度 (LatToCentiMeters=11132000, LonToCentiMeters=9543000)
  - 调用 final_target_coordinates()

### 步骤四：最终目标坐标计算 ✅
- `final_target_coordinates()` 已完成：
  - 边界检查（投弹点方圆 20m）
  - 3s 时间窗口收集目标
  - ≥20 帧取均值作为最终坐标
  - 赋值 drop_coordinates_lat/lon

### 步骤五：saveToFile() ✅
- 保存像素坐标、目标坐标、最终坐标、无人机状态
- 30s 定时触发，输出到 /home/lckfb/catkin_ws/savedata/

### 编译 ✅
- 三文件联合编译：coordinate_cal.cpp + drop_boom.cpp + waypoint_cal.cpp
- `catkin_make` 编译成功
- `.bashrc` 已更新为 test4 工作空间

## 待完成
- RK3566 部署 coordinate_cal 包
- drop_boom.cpp 的 fire() 函数（实验六）
- waypoint_cal.cpp 的航点修改（实验五）
